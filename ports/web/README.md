# Web port

`index.html` is the browser-based version of SNAKE! and the clearest reference
for the game's original rules and pacing. It is a self-contained HTML/Canvas
implementation with no build step.

From the repository root, serve the project over HTTP:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000/ports/web/`.
